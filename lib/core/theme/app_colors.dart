import 'package:flutter/material.dart';

class AppColors {
  static const background   = Color(0xFF0A0A0A); // near-black
  static const surface      = Color(0xFF141414);
  static const surfaceHigh  = Color(0xFF1E1E1E);
  static const divider      = Color(0xFF1E1E1E);

  static const textPrimary   = Color(0xFFEDEDED);
  static const textSecondary = Color(0xFF6B6B6B);

  static const accent   = Color(0xFF10B981); // muted emerald
  static const accentFg = Color(0xFF04130D); // dark green-black for text on accent

  static const alarm     = Color(0xFFEF4444); // calm red
  static const recurring = Color(0xFF2DD4BF); // teal
  static const reminder  = Color(0xFF38BDF8); // soft sky blue
  static const todo      = Color(0xFF34D399); // soft emerald
  static const online    = Color(0xFF10B981); // muted emerald
}
